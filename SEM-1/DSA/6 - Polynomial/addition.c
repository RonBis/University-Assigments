#include <stdio.h>
#include <stdlib.h>

typedef struct Polyterm {
    int exp;
    double coeff;
} polyterm;

polyterm* input_poly() {
    int highest_exp;
    printf("Enter highest exponent: ");
    scanf("%d", &highest_exp);

    
}

int main() {
    return 0;
}
